import {test} from 'node:test';import assert from 'node:assert/strict';import {fixture} from './fixture.ts';import {discover,history,resolveRef,safeRead,tree,git} from '../packages/analyzer/git.ts';
test('local repository discovery and safe historical reads',async()=>{const f=await fixture();try{const before=await git(f.root,['status','--porcelain']);assert.equal(await discover(f.root+'/src'),f.root);const h=await history(f.root,await resolveRef(f.root,'HEAD'));assert.equal(h.length,4);assert.equal((await tree(f.root,h[0].sha)).length,1);await assert.rejects(()=>resolveRef(f.root,'--help'));await assert.rejects(()=>safeRead(f.root,'../secret'));assert.equal(await git(f.root,['status','--porcelain']),before);}finally{await f.cleanup();}});
