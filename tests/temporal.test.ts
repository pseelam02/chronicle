import {test} from 'node:test';import assert from 'node:assert/strict';import {parseFile} from '../packages/analyzer/parser.ts';import {matchLineage,compare,blast,selectCheckpoints} from '../packages/analyzer/temporal.ts';import type {Checkpoint,Commit} from '../packages/shared/model.ts';
const cp=(p:string,s:string):Checkpoint=>({ref:p,label:p,commit:{} as Commit,files:[parseFile(p,s)],edges:[]});
test('unique fingerprints follow rename and move with confidence',()=>{const a=cp('a.ts','export function sum(a:number){return a+1}');const b=cp('b.ts','export function total(a:number){return a+1}');matchLineage(a,b);assert.equal(a.files[0].symbols[0].id,b.files[0].symbols[0].id);assert.equal(compare(a,b).moved.length,1);assert.equal(b.files[0].symbols[0].confidence,.85);assert.ok(blast(a,b).length);});
test('sampling bounds and endpoints',()=>{const c=Array.from({length:100},(_,i)=>({sha:String(i),parents:[]} as unknown as Commit));const s=selectCheckpoints(c,12);assert.equal(s.length,12);assert.equal(s[0].sha,'0');assert.equal(s.at(-1)?.sha,'99');});
